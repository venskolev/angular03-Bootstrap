import { FlightBgDirective } from './flight-bg.directive';

describe('FlightBgDirective', () => {
  it('should create an instance', () => {
    const directive = new FlightBgDirective();
    expect(directive).toBeTruthy();
  });
});
